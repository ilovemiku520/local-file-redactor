[pytest]
testpaths = backend/tests
markers =
    integration: requires local model weights, OCR and/or a running project Ollama service
    font: requires the Windows Microsoft YaHei font for document rendering
